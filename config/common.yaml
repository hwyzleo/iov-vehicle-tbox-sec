# TBOX 公共配置
cloud:
  endpoint: "https://api.example.com"
  timeout_ms: 5000
  retry_count: 3
  retry_delay_ms: 1000

environment:
  is_production: false

log:
  level: "INFO"
  file: "/var/log/tbox/tbox.log"
  max_size_mb: 10
  max_files: 5
